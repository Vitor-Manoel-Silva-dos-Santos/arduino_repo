Comando para iniciar o server:

LINUX
Dentro de um terminal na pasta server, digite:
"./bootstrap.sh"

WINDOWS:
Se não tiver instalado:
pip install Flask
pip install pipenv
pipenv run flask --debug run -h 0.0.0.0
